<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pizzaria Delícia</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <!-- Menu MUITO simples -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
            <div class="navbar-nav mx-auto">
                <a class="nav-link text-dark" href="@Url.Action("Index", "Home")">Início</a>
                <a class="nav-link text-dark" href="@Url.Action("Cardapio", "Home")">Cardápio</a>
                <a class="nav-link text-dark" href="@Url.Action("Carrinho", "Home")">Carrinho</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <main>
            @RenderBody()
        </main>
    </div>

    <footer class="mt-5 py-4">
        <div class="container text-center">
            <p class="text-muted mb-0">&copy; 2025 - Pizzaria Delícia</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>